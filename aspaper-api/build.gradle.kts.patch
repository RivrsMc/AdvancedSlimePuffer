--- a/pufferfish-api/build.gradle.kts
+++ b/pufferfish-api/build.gradle.kts
@@ -40,6 +_,7 @@
 }
 
 dependencies {
+    api(project(":api")) //ASP
     // api dependencies are listed transitively to API consumers
     api("com.google.guava:guava:33.3.1-jre")
     api("com.google.code.gson:gson:2.11.0")
@@ -102,9 +_,11 @@
         java {
             srcDir(generatedDir)
             srcDir(file("../paper-api/src/main/java"))
+            srcDir(file("../pufferfish-api/src/main/java"))
         }
         resources {
             srcDir(file("../paper-api/src/main/resources"))
+            srcDir(file("../pufferfish-api/src/main/resources"))
         }
     }
 }
